#!/bin/bash
BASE="$(cd $(dirname "$0") ; cd "$(pwd -P)/../.." ; pwd -P)"
. "$(dirname "$0")/gdirs"
for dir in "${gdirs[@]}" ; do
    (
        cd "$BASE/$dir"
        printf "\ncd %s\n" "$(pwd)"
        git pull
    )
done
